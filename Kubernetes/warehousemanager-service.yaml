apiVersion: v1
kind: Service
metadata:
  name: warehousemanager-service
spec:
  selector:
    app: warehousemanager
  type: ClusterIP  
  ports:
    - protocol: TCP
      port: 9999
      targetPort: 8080
